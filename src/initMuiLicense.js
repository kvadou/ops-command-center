
import { LicenseInfo } from "@mui/x-license";

export async function initMuiLicense() {
  console.warn("[MUI X] initMuiLicense() called, but premium features are no longer in use.");
  return false;
}
